// Fixed configuration for ClusterTree visualizations

// Horizontal spacing between ranked clusters (pixels)
export const CLUSTER_RANKING_SPACING = 120;
export const CLUSTER_RANKING_SPACING_MOBILE = 100;

// Maximum number of characters for cluster labels under ticks
export const CLUSTER_RANKING_MAX_LABEL_CHARS = 18;
export const CLUSTER_RANKING_MAX_LABEL_CHARS_MOBILE = 14;

// Padding on each side of the ranking content to avoid label clipping (pixels)
export const CLUSTER_RANKING_EDGE_PADDING = 60;
export const CLUSTER_RANKING_EDGE_PADDING_MOBILE = 40;

// Static node radius for ranked cluster circles (pixels)
export const CLUSTER_RANKING_NODE_RADIUS = 18;
export const CLUSTER_RANKING_NODE_RADIUS_MOBILE = 14;

// Extra vertical space reserved for labels/axis in the ranking SVG (pixels)
export const CLUSTER_RANKING_AXIS_SPACE = 46;
